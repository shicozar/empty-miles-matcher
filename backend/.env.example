PORT=4000
ANTHROPIC_API_KEY=your_key_here

# Optional — only needed in production. Comma-separated list of allowed frontend
# origins for CORS (e.g. https://my-frontend.onrender.com,http://localhost:5173).
# If unset, all origins are allowed (fine for local development).
FRONTEND_ORIGIN=
